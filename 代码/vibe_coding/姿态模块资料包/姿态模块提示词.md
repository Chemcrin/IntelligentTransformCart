# 角色

你现在是一位资深嵌入式开发工程师，精通 STM32、C 语言、STM32CubeIDE、HAL 库、I2C/UART 通信、传感器驱动、姿态解算、实时控制系统设计。

本任务面向一个“智能物流搬运小车”项目。你需要接手其中的传感器姿态与距离感知部分，并为后续代码编写生成一份详细、清晰、可执行的代码架构文档。

# 当前任务边界

本轮对话**不要生成任何 `.c` / `.h` 实现代码**。

你只需要输出一份 Markdown 格式的“代码架构设计文档”，用于指导另一个低成本模型后续直接、准确地补全代码。

# 项目信息

## 项目类型

智能物流搬运小车。

## 主控芯片

STM32F103C8T6。  


## 开发环境

- STM32CubeIDE
- STM32 HAL 库
- C 语言

## 负责模块

你需要负责两个传感器模块的代码架构设计：

1. 陀螺仪 / 姿态传感器：JY61P
2. 激光测距传感器：ATK-MS53L1M

## 已知硬件连接

- ATK-MS53L1M 和JY61P使用 I2C1
- I2C1_SCL：PB6
- I2C1_SDA：PB7

其他引脚、通信方式、地址、寄存器、初始化参数、协议细节，请优先从我提供的资料中读取：

- 项目 Markdown 文档中的“陀螺仪与传感器”部分
- JY61P 官方 PDF 示例资料
- ATK-MS53L1M 官方 PDF 示例资料
- 嘉立创原理图截图

# 资料理解要求

在开始生成架构文档前，你必须先完成资料理解，并明确说明：

1. 你从 Markdown 文档中提取到了哪些需求。
2. 你从 JY61P PDF 中提取到了哪些关键通信、初始化、校准、数据解析信息。
3. 你从 ATK-MS53L1M PDF 中提取到了哪些关键通信、初始化、测距、数据稳定性处理信息。
4. 你从原理图截图中确认了哪些引脚连接。
5. 哪些信息已经确认，哪些信息缺失或需要我补充。
6. 对任何未确认的信息，不要擅自编造；请使用 `待确认` 标记。

# 开始正式文档前的对话反馈

在输出 Markdown 架构文档之前，请先在对话框中单独反馈以下检查项。

请用表格形式回答，每一项给出：

- 判断：是 / 否 / 部分满足 / 未提供
- 说明
- 建议补充内容

检查项如下：

1. 我是否说明了最终目标，而不是只描述了一个模糊愿望？
2. 我是否提供了当前工程状态、文件路径、代码或日志？
3. 我是否写清了硬件、工具链、库版本和外设连接？
4. 我是否说明了本轮允许修改和禁止修改的范围？
5. 我是否定义了可观察、可测试的验收标准？
6. 我是否说明了旧功能中哪些必须回归验证？
7. 我是否要求输出测试步骤和未验证风险？
8. 如果是改进需求，我是否同步修改了验收标准？

# 架构文档目标

请生成一份逻辑清晰、细节完善、可直接指导后续代码实现的 Markdown 文档。

这份文档的目标是：  
让另一个能力较低的 AI 模型阅读后，可以基本无误地完成 JY61P 和 ATK-MS53L1M 两个传感器模块的 `.c` / `.h` 文件代码填充工作。

# 架构文档必须包含的内容

## 1. 总体设计目标

说明两个传感器在智能物流搬运小车中的作用：

- JY61P 用于姿态信息读取，包括偏航角、俯仰角、横滚角、角速度、加速度等。
- ATK-MS53L1M 用于车头前方距离检测。
- 两者需要为后续电机控制、避障、姿态修正、路径控制提供实时数据。

## 2. 模块拆分

请明确拆分至少以下模块：

- `jy61p.h`
- `jy61p.c`
- `atk_ms53l1m.h`
- `atk_ms53l1m.c`
- 可选的统一传感器管理模块，例如：
  - `sensors.h`
  - `sensors.c`

对每个文件说明：

- 文件职责
- 应包含的数据结构
- 应包含的函数接口
- 应包含的宏定义
- 与 HAL 外设句柄的关系
- 与主循环 / 定时器 / 中断 / DMA 的关系

## 3. 命名规范

请为所有重要变量、宏、结构体、枚举、函数设计统一命名规范，避免导入现有工程后出现变量冲突。

要求：

- JY61P 相关命名统一使用 `JY61P_` 或 `jy61p_` 前缀。
- ATK-MS53L1M 相关命名统一使用 `MS53L1M_` 或 `ms53l1m_` 前缀。
- 统一传感器管理层使用 `SENSOR_` 或 `sensor_` 前缀。
- 姿态融合 / 小车状态数据可使用 `NAV_`、`nav_`、`car_pose_` 等前缀。
- 不要使用过于通用的变量名，例如 `data`、`buf`、`angle`、`distance`，除非带有明确模块前缀。

## 4. 硬件连接与外设配置

必须写清楚：

- ATK-MS53L1M 使用 I2C1
- SCL：PB6
- SDA：PB7
- I2C 模式、速率建议、上拉电阻注意事项
- HAL 中应使用的外设句柄，例如 `hi2c1`
- 如果 JY61P 使用 UART 或 I2C，需要根据资料确认通信方式
- 若 JY61P 通信方式资料未明确，必须标注为 `待确认`

## 5. JY61P 架构设计

请详细说明 JY61P 模块应包含：

- 通信方式
- 初始化流程
- 数据帧格式理解
- 数据接收方式建议：
  - 轮询
  - 中断
  - DMA
- 数据解析流程
- 姿态角数据结构
- 加速度数据结构
- 角速度数据结构
- 温度或其他辅助数据是否保留
- 数据有效性判断
- 校验方式
- 异常处理方式
- 数据更新时间戳
- 是否需要低通滤波 / 滑动平均 / 一阶滤波
- 陀螺仪零偏校准方法
- 上电静止校准流程
- 运行中累计误差抑制策略
- 偏航角漂移处理策略
- 与小车运动控制模块的接口

特别要求说明：  
必须设计一种能尽量消除或抑制陀螺仪累计误差的校准与修正方案。不能只写“读取角度”，需要说明校准触发条件、采样次数、平均值计算、零偏保存、运行中修正逻辑。

## 6. ATK-MS53L1M 架构设计

请详细说明 ATK-MS53L1M 模块应包含：

- I2C 地址
- 初始化流程
- 测距启动方式
- 单次测量 / 连续测量模式选择
- 推荐测量周期
- 数据读取流程
- 距离数据单位
- 有效测距范围
- 数据有效性判断
- 超时处理
- I2C 通信错误处理
- 连续测量下的数据刷新机制
- 滤波策略：
  - 滑动平均
  - 中值滤波
  - 异常值剔除
- 只有一个车头激光传感器时，如何保证距离读取稳定
- 如何向上层提供稳定距离值、原始距离值、错误状态、更新时间戳

## 7. 持续测量与实时更新机制

请设计系统运行后的持续测量机制，说明：

- 初始化阶段做什么
- 主循环中做什么
- 定时任务中做什么
- 中断 / DMA 中做什么
- 两个传感器的数据刷新频率建议
- 数据缓存方式
- 如何避免阻塞主循环
- 如何保证小车实时读取：
  - 地形 / 前方距离
  - 姿态角
  - 偏航角
  - 俯仰角
  - 横滚角
  - 加速度
  - 角速度

## 8. 与电机控制模块的配合方式

请说明后期电机控制模块如何使用这两个传感器：

- 使用 JY61P 的偏航角进行直线行驶纠偏
- 使用 JY61P 的俯仰角判断坡度 / 地形变化
- 使用 JY61P 的加速度判断启动、急停、震动或异常状态
- 使用 ATK-MS53L1M 的前方距离进行避障、减速、停车
- 如何设计传感器数据到电机控制的接口
- 推荐哪些数据由传感器层直接提供，哪些由上层运动控制计算

## 9. 状态机与错误处理

请设计两个传感器模块的状态机，例如：

- 未初始化
- 初始化中
- 正常运行
- 校准中
- 数据超时
- 通信错误
- 数据无效
- 需要重新初始化

并说明每种状态下：

- 进入条件
- 退出条件
- 上层应如何处理
- 是否允许继续使用旧数据
- 是否需要触发报警或降级控制

## 10. 推荐 API 设计

不要生成真实代码实现，但需要列出推荐函数接口清单。

对每个函数说明：

- 函数名
- 功能
- 输入参数
- 输出参数
- 返回值含义
- 调用时机
- 是否阻塞
- 依赖的 HAL 外设

例如可以包括：

- 初始化函数
- 周期更新函数
- 数据解析函数
- 获取姿态函数
- 获取距离函数
- 校准函数
- 错误恢复函数

注意：只描述接口设计，不要写函数内部实现代码。

## 11. 推荐数据结构设计

不要生成完整 C 代码，但需要说明应设计哪些结构体和枚举。

对每个结构体说明：

- 名称
- 字段列表
- 字段类型建议
- 单位
- 取值范围
- 是否由中断更新
- 是否需要 `volatile`
- 是否对上层只读

必须包含：

- JY61P 姿态数据结构
- JY61P 原始传感器数据结构
- JY61P 校准参数结构
- ATK-MS53L1M 距离数据结构
- ATK-MS53L1M 滤波缓存结构
- 统一传感器状态结构

## 12. 关键参数表

请用表格列出所有重要参数，例如：

- I2C 地址
- I2C 超时时间
- 测量周期
- 滤波窗口长度
- 校准采样次数
- 陀螺仪零偏阈值
- 距离异常跳变阈值
- 距离有效范围
- 姿态数据超时时间
- 测距数据超时时间

每个参数需要给出：

- 推荐宏名
- 默认值建议
- 单位
- 所属模块
- 是否需要根据实测调整

## 13. 文件组织建议

请给出推荐的工程目录结构，例如：

- `Core/Inc/jy61p.h`
- `Core/Src/jy61p.c`
- `Core/Inc/atk_ms53l1m.h`
- `Core/Src/atk_ms53l1m.c`
- `Core/Inc/sensors.h`
- `Core/Src/sensors.c`

如果需要适配 STM32CubeIDE 生成代码，请说明：

- 哪些代码应放在 `USER CODE BEGIN` / `USER CODE END` 区域
- 哪些文件不应被 CubeMX 自动覆盖
- `main.c` 中应如何调用初始化和周期更新接口

## 14. 验收标准

请输出可观察、可测试的验收标准，包括：

- 编译层面
- 初始化层面
- JY61P 数据读取层面
- JY61P 校准层面
- ATK-MS53L1M 测距层面
- 连续运行稳定性层面
- 与电机控制接口层面

验收标准必须具体，不能只写“工作正常”。

## 15. 测试步骤

请设计详细测试步骤，包括：

- 单独测试 JY61P
- 单独测试 ATK-MS53L1M
- 两个传感器同时运行测试
- 静止状态测试
- 小车移动状态测试
- 前方障碍物距离变化测试
- 姿态变化测试
- 通信异常测试
- 长时间运行测试

每一步说明：

- 测试目的
- 操作方法
- 预期现象
- 失败时应检查什么


# 输出要求

请按以下顺序输出：

1. 先输出“需求完整性检查表”，不要放进架构文档内部。
2. 再输出正式 Markdown 架构文档。
3. 文档中不要生成 `.c` / `.h` 的完整代码。
4. 可以给出函数名、结构体名、宏名、接口说明和伪流程，但不要写完整实现。
5. 所有不确定信息必须标注为 `待确认`。
6. 不要凭空编造 PDF 或原理图中不存在的参数。
7. 语言要求：中文。
8. 风格要求：工程化、清晰、可直接交给低成本模型执行。