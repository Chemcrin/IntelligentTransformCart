
 > 时间： 2026年6月15日09:13:14
 > 任务：综合[项目审查报告_2026-06-09](../Docs/项目审查报告_2026-06-09.md) 出现的问题，解决当前工程出现的错误，以及实现未实现的功能

# 1. 修改重要错误

## 一. 有关uart串口分配的错误

## （1）错误
根据[项目审查报告_2026-06-09](../Docs/项目审查报告_2026-06-09.md)所写，电机的 *Modbus 电机驱动* 和*RS485*模块均使用了uart3进行串口通信。这是一个**严重的错误**。因为早在[智能物流小车-硬件平台技术手册V3](../../智能物流小车-硬件平台技术手册V3.md)中的硬件设计就已经规定了电机的通信必须使用**uart1**，这是由PCB绘制时就定死的硬件设计，不能在代码设计时随意更改。
## （2）来源
经检查，错误信息最早出现于[麦轮运动控制系统设计文档5Pro](../../麦轮运动控制系统设计文档5Pro.md)以及后来的[麦轮模块框架](../麦轮框架项目设计资料包/麦轮模块框架.md)。导致Agent填充代码时根据错误的文档信息生成了错误的代码
## （3）任务
请检查 `smart_logistics_car_integrated` 里所有临时 UART3 传感器回传测试文件与模板，判断它们是否还会与正式运行方案冲突。检查可能会误占 `USART3`、容易被误并入 `main.c`、或与UART1 步进调整相冲突的文件。
请直接检查 `smart_logistics_car_integrated` 中所有与步进电机相关的 UART 绑定，把原先误挂在 `UART3/USART3` 的代码迁移到 `UART1/USART1`先列出必须修改的文件和每处修改原因，再给出是否会影响现有麦轮电机、激光急停、传感器回传和树莓派遥控链路的判断；如果能明确改法，就继续输出修改后的代码。
任务的验收标准是：生成以uart1作为电机串口通信的代码，uart3保持空置

## 二. 修改参数
按照审查情况，修改main.c里的ATK-MS53L1M/VL53L1X 测距参数，确保功能能够正常运行
调整激光传感器有关急停的相关代码，按照刚刚完成的审查，统一急停距离至10cm，保证小车不会在离障碍物较远时触发急停

## 三. 审查不符合规范的变量名，统一修改
不修改现有变量，而是在工程完成后额外生成一份《工程变量总览》的Markdown文档，用于说明现有变量，我将在后续更新硬件手册

## 四. RS485 方向
不做更改，保持现状
因为开发板上与电机的RS485模块只有uart1的RX、TX相连，没有分配GPIO引脚作为方向控制。如果后续pid调参一定需要增加一个GPIO的方向控制，请说明使用了哪个引脚，并且告知我进行飞线
# 2. 清除未加入的模块

之前的测试时加入了一些不必要的代码模块尚未删除，需要检查是否存在工程残留，包括
## （1）`sensor_uart3_stream_test`  (ttl串口传输测试)
以下是与GPT的部分对话：

该模块是一个临时模块，加入时的操作是: 临时替换/合并到 IDE 工程

| IDE 工程位置                     | 操作                                               |
| ---------------------------- | ------------------------------------------------ |
| `uart3_sensor_stream_temp.h` | 新增                                               |
| `uart3_sensor_stream_temp.c` | 新增并加入编译                                          |
| `main.c`                     | 临时按 `main_uart3_sensor_stream_temp.c` 的逻辑修改用户代码区 |
| Include Path                 | 临时加入 `../UserModules/uart3_sensor_stream_temp`   |

注意：这次测试会占用 `USART3 PB10/PB11` 给树莓派回传数据，因此测试期间不要初始化 `Mecanum_Init(&huart3)`，也不要运行电机 RS485 链路。正式小车运行时，把 `Core/Src/main.c` 恢复为正式版本，并移除/禁用 `uart3_sensor_stream_temp.c` 编译即可。


需要检查是否有该工程项目的残留，如果存在残留，请按照对话内容，在不影响其他代码功能的情况下移除，如果没有，请忽略
此模块的功能将会在后面的CSV数据传输中用更好的方法实现

## （2）上车联调模式

该模式已经测试完成，可以删除

以下是与GPT的部分对话，包含了项目刚刚完成时修改的文件


| 类型      | 文件                          | 说明                                                                  |
| ------- | --------------------------- | ------------------------------------------------------------------- |
| 新增模块头文件 | [bringup_mode.h]            | 上车联调模式对外 API、配置和报告结构                                                |
| 新增模块源文件 | [bringup_mode.c]            | 地址检查、逐个电机点动、四轮方向校验、激光急停验证流程                                         |
| 更新头文件   | [motor_manager.h]           | 新增 `MotorManager_GetAddress()` 和 `MotorManager_SetSingleWheelRpm()` |
| 更新源文件   | [motor_manager.c]           | 实现单轮点动和轮位地址查询                                                       |
| 更新示例入口  | [main_integrated_example.c] | 增加 `APP_ENABLE_BRINGUP_MODE` 示例调用入口                                 |
| 新增说明文档  | [上车联调模式说明.md]               | 调试入口、串口输出、现场流程说明；不参与编译                                              |
|         |                             |                                                                     |

IDE 内还要记得做两件事：

|IDE 配置项|操作|
|---|---|
|Include Path|加入 `UserModules/bringup_mode`|
|编译源文件|把 `UserModules/bringup_mode/bringup_mode.c` 加入 Build Sources|
同样，检查该模块是否残留



# 3. 增加新功能

## 一. 闭环直线控制
为小车现有行驶代码的基础上，增加通过pid控制的闭环控制，保证小车行驶时不偏移方向
```
1. 控制目标
- 小车在前进或后退时能够保持直线运行。
- 当左右轮速度不一致时，系统能够自动修正 PWM 输出。
- 支持设置目标速度

2. 控制策略
- 使用左右轮编码器速度作为反馈。
- 使用 PI 或 PID 控制算法进行速度闭环。
- 可以采用“基础速度 + 左右差速修正”的方式：
- correction 由左右轮速度差计算得到。

3. 代码要求
- 请给出完整、清晰的 C 语言实现。
- 包括 PID/PI 控制结构体、初始化函数、计算函数。
- 包括直线控制函数
- 包括停止函数
- 请说明这些函数应该放在主循环中调用，还是放在定时器中断中周期调用。
- 能够结合我的现有工程运动控制以及变量名规范
- 尽量保持代码模块化，方便后续移植到我的工程中。

4. 输出内容
- 给出核心代码。
- 解释每个函数的作用。
- 给出 PID 参数初始建议。
- 给出调试步骤，例如如何调整 Kp、Ki、Kd。
- 说明如何避免积分饱和、PWM 超限和低速抖动。
```

## 二. CSV数据回传
### CSV任务书：为物流小车 STM32 固件添加全量 CSV 调试流

#### 1. 硬件背景

- 主控：STM32F103
    
- 姿态传感器：JY61P，通过 **I2C1** 通信（地址 `0x50`，已调通）
    
- 调试输出串口：**UART1**，波特率 `115200`，8N1
    
    - 调试时通过 TTL‑USB 模块连接到 PC
        
    - 实际比赛时连接到树莓派的 UART（物理连线可互换）
        

#### 2. 功能需求

新增一个**条件编译的全量传感器数据 CSV 输出功能**，开关宏为 `CSV_DEBUG_ENABLE`（定义在 `config.h` 中）。  
当 `CSV_DEBUG_ENABLE == 1` 时：

- 每次从 JY61P 读取到一帧完整的传感器数据后，在 main 循环中**间隔发送**一行 CSV 文本到 UART1。
    
- 当 `CSV_DEBUG_ENABLE == 0` 时，**所有相关代码不占用任何 ROM/RAM/CPU 时间**。
    

#### 3. CSV 数据格式

每一行输出的字段顺序固定，均为浮点数（保留 3 位小数），英文逗号分隔，以 `\r\n` 结尾：

```text

时间戳(ms), 加速度X(g), 加速度Y(g), 加速度Z(g), 角速度X(dps), 角速度Y(dps), 角速度Z(dps), Roll(°), Pitch(°), Yaw(°)
```
示例：

```text

123456, -0.012, 0.034, 1.005, 0.23, -0.15, 0.08, 1.2, -3.4, 45.6
```
- 时间戳：`HAL_GetTick()` 的 32 位值。
    
- 加速度单位：g（重力加速度），需从原始寄存器值换算。
    
- 角速度单位：°/s。
    
- 姿态角单位：度，取 JY61P 融合后的角度。
    

#### 4. 输出频率与位置

- **位置**：在 main 函数的 `while(1)` 循环内，紧接在 `JY61P_ReadData()` 函数之后。
    
- **频率控制**：每进入 10 次主循环才发送一行（即 100Hz 主循环 → 10Hz CSV 输出）。  
    实现方式：静态计数器，取模10。
    

#### 5. 串口发送实现要求

- 优先使用 **HAL_UART_Transmit_DMA** 或 **HAL_UART_Transmit_IT**，避免长时间阻塞。
    
- 如果暂时使用阻塞 `printf` 也可以接受，但必须在代码注释里标记 `TODO: 改为 DMA 发送`。
    
- 需要配置 UART1 的初始化代码（`MX_USART1_UART_Init`），波特率 921600，并确保对应的 GPIO 已正确配置。
    

#### 6. 其它约束

- 格式化用的临时缓冲区长度 ≥ 128 字节，用局部数组。
    
- 不修改现有的 I2C1 驱动和主控制逻辑。
    
- 所有新增的发送代码必须放在 `#if CSV_DEBUG_ENABLE` 和 `#endif` 之间。
    
- 提交前在 `config.h` 中将 `CSV_DEBUG_ENABLE` 设置为 `1`（调试阶段默认开启）。


### 另：需要完成的细节
### 1. 创建一个条件编译开关

在全局头文件（比如 `config.h`）中加入：

```c

#define CSV_DEBUG_ENABLE  1   // 1: 开启全量CSV输出; 0: 关闭

```

所有 CSV 相关代码都用 `#if CSV_DEBUG_ENABLE` 包起来，这样比赛时一键关闭，不占任何资源。

### 2. 决定要输出的数据字段（全量）

JY61P 通过 I2C 能读到的数据通常包括（以 16 位有符号数给出）：

- 三轴加速度原始值 / 转换后的 mg 值
    
- 三轴角速度原始值 / 转换后的 °/s
    
- 三轴姿态角（Roll, Pitch, Yaw）—— 这是模块内部已融合好的
    
- 可选：温度、四元数
    

我建议的 CSV 列顺序和格式（每行一个数据帧）：

text

timestamp_ms, accelX, accelY, accelZ, gyroX, gyroY, gyroZ, roll, pitch, yaw

示例数据：

text

12345, 0.012, -0.034, 1.001, 0.23, -0.15, 0.08, 1.2, -3.4, 45.6

- `timestamp_ms` 用系统运行毫秒数（`HAL_GetTick()`），方便绘图。
    
- 数值使用**浮点**，直接保留小数点后 3~4 位，上位机可直接导入 Excel。
    
- 各列之间用逗号分隔，行末用 `\r\n`。
    

### 3. 串口输出配置

- **UART1**，波特率建议 **921600** 或 **460800**（高频率输出时 115200 可能不够快，如果控制周期 100Hz 且只输出一行约 100 字节，921600 很轻松）。
    
- 若使用 115200，则必须降低输出频率（如每 5 帧发一次）。
    
- **发送方式**：尽量用 DMA 或中断方式发送，避免 `printf` 的死等。但初期调试可直接重定向 `printf` 到 UART1，后面再优化。
    

### 4. 输出频率与触发位置

**关键：不能在每个控制周期都发送全量 CSV，否则会严重降低主控实时性。**  
必须加入分频输出逻辑，例如：

c

static uint32_t csv_counter = 0;
if (csv_counter++ % 10 == 0) {  // 每10个控制周期发一次
    SendCsvLine(&sensor_data);
}

- 如果主循环频率 100Hz，则 CSV 输出频率降为 10Hz，对人眼观察和后期分析已经足够。
    
- 将 `SendCsvLine()` 放在读取完传感器数据之后的 main 循环里，不要在中断里。
    

### 5. 缓冲区与栈保护

CSV 格式化会用到 `sprintf` 或 `snprintf`，需要较大的临时 buffer（例如 128 字节）。  
在工程设置中把堆栈大小适当增大（如 main 栈改为 2048 字节，任务栈也调大），并在代码中使用局部数组而非大量全局变量。

## 三. 树莓派数据下发
请基于 `smart_logistics_car_integrated` 直接实现树莓派到 STM32 的串口控制链路：新增 `UserModules/remote_cmd`，在 `main` 主循环里修改相关内容，通过 USART2 接收树莓派下发的速度/停机/清零指令，并加入心跳超时急停、激光急停优先级和基础状态回传。请按当前工程结构输出需要新增或修改的代码文件。同时，实现激光传感器和陀螺仪各项数据的回传：也通过uart2，将这些数据传输给树莓派







