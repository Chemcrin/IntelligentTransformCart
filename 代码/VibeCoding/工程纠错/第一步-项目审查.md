现在你是一位优秀的嵌入式开发者，接手了一个开发一半的物流小车项目。之前已经完成了一些有关麦轮模块和传感器模块的代码，但是仍然存在着一些问题，需要你先查阅工程的相关进度。
该工程使用HAL库，是一个基于stm32F103C8T6的小车项目，使用stm32CubeIDE开发
包含的硬件有：通过RS485 通信的4个ZDTX42 电机、麦克纳姆轮；ATK-MS53L1M激光传感器和JY61P陀螺仪
现在请将[智能物流小车-硬件平台技术手册V3](../../智能物流小车-硬件平台技术手册V3.md)作为小车的最高硬件标准，进行项目审查，理解项目内容。同时，查找项目是否有违背该手册的部分。审查完成后，输出一份Markdown文本，用于解释项目内容。
小车项目在zip文件夹内

# 第二步-修改问题
> 根据审查报告，进行问题的整改
> 参考资料：[项目审查报告_2026-06-09](../Docs/项目审查报告_2026-06-09.md) 6.9第一次审查出的可实现功能及问题；[工程修改任务书](工程修改任务书.md)作为修改的任务条例，逐条完成


根据所给出的Markdown文档，以及刚刚完成的项目文档，启动plan模式，根据[工程修改任务书](工程修改任务书.md)给出的项目修改细则，逐步完成任务
如果在修改项目时遇到问题，请先停止修改，并向我询问具体修改方式
验收标准：直接生成一份修改完成的IDE格式文件夹，保证导入IDE工作区后、不修改任何文件的情况下可以直接烧录使用。再额外生成一份介绍项目变动的Markdown文件，详细说明做了什么修改

# 第三步-新功能
## 1. 新增比赛路径
请在 `smart_logistics_car_integrated` 里新增一个用于固定路线运行的轻量路径执行模块，放到 `UserModules` 下，要求尽量复用 `Sensor_GetSnapshot()`、`Mecanum_SetVelocity()`、`ChassisControl` 和现有急停逻辑，先支持“直行/横移/原地旋转/停车等待”四类动作，能按预设步骤顺序执行，并在激光急停或链路异常时立即中止。请先结合当前工程结构给出接入点和最小实现方案，然后输出需要新增或修改的代码文件。
路径可拆分为为以下任务：执行向前3300mm，然后逆时针原地旋转90°，再向前走1000mm， 再原地逆时针旋转90°然后再直行1000mm，然后再退回到前一个位置，往前走500mm，再向右平移350mm然后往前走1500mm向左平移400mm在向前走700mm
根据现有工程架构，生成新增的.c/.h文档，并且修改现在的main.c文件，把新功能加入。验收标准是生成可以直接替换原文件的main.c文件和新增的.c/.h文件，要求直接新增、替换原文件即可直接烧录