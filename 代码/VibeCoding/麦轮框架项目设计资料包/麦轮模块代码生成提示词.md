# 角色

你现在是一位资深嵌入式开发工程师，精通 STM32、C 语言、STM32CubeIDE、HAL 库运动系统设计。

# 任务
本任务面向一个“智能物流搬运小车”项目。你需要接手其中的电机驱动与麦轮移动部分，并根据现有代码架构文档，按照cubeIDE的格式，生成一份简洁、可用、稳定的代码文件，并将整理完成的代码文件夹 MovingSystem61 放在目录D:\AI_agent\Code下。按stm32cubeIDE的工程格式存放代码，包括core文件夹下的src和inc文件夹。
# 硬件架构
现有的硬件是一块stm32F103C8T6开发板，以及通过uart3的PB10\PB11引脚连接在32开发板上的4个ZDTX42V2电机，电机上都有RS485模块，通过RS485总线和开发板通信。

# 项目要求与验收标准
请根据[麦轮模块框架](麦轮模块框架.md)这份文档进行代码的填充，需要你根据硬件的实际情况（注意小车由陀螺仪标定的三维方向），完成电机的差速控制实现麦轮组的全向运动的代码书写（包括前后左右的基础运动和斜方向的矢量合成），遵守框架里面提供的功能与变量名要求，同时代码填写时注意[麦轮模块代码生成细节说明](麦轮模块代码生成细节说明.md)里面提到的细节。验收标准是：能够按照md文件的里的要求完成代码任务，代码清晰简洁可用，能够持续让小车实现稳定可控万向运动，也可由更高控制层调用相关函数，直接指挥小车运动；.h/.c 文件独立清晰，可在后期多模块整合时快速整合进小车的整体代码。

# 工程自查
在生成代码前，请回复我是否有不理解不明白的部分，如果信息全面请开始填充代码。

# 项目收尾
在代码生成完成后，整理一份工程说明md文档，告知我项目都包含了什么代码文件，以及告诉我怎样导入cubeIDE，还有ioc里的引脚配置方式



# 角色
你现在是一位资深嵌入式开发工程师，精通 STM32、C 语言、STM32CubeIDE、HAL 库以及运动控制系统设计。

# 任务
接手“智能物流搬运小车”项目中的电机驱动与麦轮移动部分。请根据我提供的架构文档内容，生成一份简洁、稳定、可用的代码，并按照 STM32CubeIDE 工程格式组织文件结构。你需要输出所有代码文件的完整内容。并在最后附上清晰的Markdown说明文档，包括包含了什么代码文件，如何导入cubeIDE，还有ioc里的引脚配置方式。

# 硬件架构
- 主控：STM32F103C8T6
- 电机：4 个 ZDT_X42_V2 电机，各带 RS485 模块，通过 RS485 总线挂接
- 通信：使用 UART3，引脚 PB10、PB11与电机通信

# 输入文档
1. **麦轮模块框架**（下文提供）  
   [麦轮模块框架](麦轮模块框架.md)
2. **麦轮模块代码生成细节说明**（下文提供）  
   [[麦轮模块代码生成细节说明](麦轮模块代码生成细节说明.md)

# 项目要求
1. 根据框架文档中规定的功能、接口、变量名进行代码填充。
2. 实现基于麦轮差速模型的全向运动控制：包含前后左右基本运动，以及斜向运动的矢量合成。
3. 坐标系参照陀螺仪标定的车体方向（车头方向为 Y 轴，左为 X轴，上为 Z 轴”）。
4. 遵守代码生成细节说明中的所有约束。
5. 代码需保证小车能实现稳定可控的万向运动，且提供清晰的上层调用接口，方便后续整合。

# 交付物格式
- 将所有代码文件以文本形式输出，标明文件路径（例如 `Core/Inc/mecanum.h`、`Core/Src/mecanum.c` 等）。
- 最后输出一份工程说明的 Markdown 文档，内容包括：
  - 项目包含的代码文件清单
  - 如何将代码导入现有的 STM32CubeIDE 工程
  - 对应的 IOC 引脚配置说明（重点说明 UART3 的 PB10/PB11 配置，以及其他必须的外设初始化）

# 执行步骤
1. 先检查上述输入文档是否完整、是否存在不明确之处，如有，请明确指出并停止生成。
2. 若信息充分，请直接生成所有代码及说明文档，无需额外确认。