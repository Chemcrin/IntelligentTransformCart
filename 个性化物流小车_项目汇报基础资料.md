# 个性化物流小车——项目汇报基础资料

> 本文档基于项目推进过程资料、技术文档、工程审查报告及多个版本的代码迭代整理而成，用于指导后续 PPT 制作。
> 整理时间：2026-07-05 | 整理人：星尘光 StaRacy

---

## 一、项目概述

### 1.1 项目名称
智能物流搬运小车（个性化物流小车 / Intelligent Transform Cart）

### 1.2 项目定位
基于 STM32 + 树莓派的智能物流小车，实现自主导航与物料搬运功能，面向物流仓储场景的智能化解决方案。

### 1.3 硬件平台总览

| 模块 | 型号/规格 | 接口方式 | 功能定位 |
|------|----------|---------|---------|
| **主控 MCU** | STM32F103C8T6 | — | 底层运动控制、传感器数据采集、电机驱动 |
| **协处理器** | Raspberry Pi 4B | USART2（通过 CH340N） | 视觉识别、上位机调度、路径规划 |
| **底盘电机 ×4** | ZDT-X42-V2 闭环步进电机 | RS485 / Modbus-RTU（USART1） | 四轮独立驱动 |
| **轮组** | 75mm 麦克纳姆轮（X 形布局） | — | 全向移动 |
| **姿态传感器** | JY61P 陀螺仪 / IMU | I2C1 | 航向角测量、直线纠偏 |
| **激光测距** | ATK-MS53L1M / VL53L1X | I2C1 | 避障与距离检测（项目后期移除） |
| **通信** | RS485 半双工总线 + USART1 | — | 电机 Modbus 通信 |
| **调试/控制** | USART2（CH340N → TypeC） | — | 树莓派上下位机通信 |

### 1.4 软件架构概览

```
┌─────────────────────────────────────────────────┐
│                  主循环 (10ms)                    │
├─────────────────────────────────────────────────┤
│  传感器读取 → IMU 更新 → 路线执行 → 电机下发      │
│  (sensors.c)  (imu_app.c)  (competition_route.c) │
│                        → (chassis_control.c)     │
│                        → (mecanum_kinematics.c)   │
│                        → (motor_manager.c)       │
│                        → (zdt_x42_modbus.c)      │
│                        → (modbus_rtu.c)          │
└─────────────────────────────────────────────────┘
```

项目采用**模块化分层设计**，从高层次到底层依次为：

1. **路线执行层**（`competition_route`）：按预设步骤执行路径
2. **底盘控制层**（`chassis_control`）：航向修正、速度规划
3. **运动学层**（`mecanum_kinematics`）：麦轮正/逆运动学解算
4. **电机管理层**（`motor_manager`）：四电机命令缓存与调度
5. **协议层**（`zdt_x42_modbus` + `modbus_rtu`）：Modbus 帧封装与 RS485 收发

---

## 二、项目推进过程

### 第一阶段：项目启动与硬件选型（2026-05-22 前）

- 确定项目方向：物流搬运场景的智能小车
- 硬件选型：STM32F103C8T6 主控、树莓派 4B、ZDT-X42-V2 步进电机、麦克纳姆轮
- 建立项目目录结构，初始化 Git 仓库
- 完成核心传感器（JY61P 陀螺仪、ATK-MS53L1M 激光）的技术调研

### 第二阶段：基础驱动开发与传感器驱动（2026-05-22 ~ 2026-05-29）

- 完成 JY61P 陀螺仪 UART 驱动开发与验证
- 完成 ATK-MS53L1M / VL53L1X 激光测距驱动移植
- 建立传感器数值读取框架
- 完成硬件平台技术手册 V1~V3 迭代

**关键节点：**
- v0.1.0（2026-05-22）：项目初始化，陀螺仪 UART 驱动、PCB 目录搭建
- v0.1.1（2026-05-29）：代码重构，新增 STM32 传感器驱动库，麦轮运动学框架

### 第三阶段：麦轮底盘控制开发（2026-05-29 ~ 2026-06-09）

- 完成麦轮逆运动学解算模块（`mecanum_kinematics.c`）
- 完成 ZDT 电机 Modbus-RTU 协议封装（`zdt_x42_modbus.c`）
- 完成四电机管理与同步控制（`motor_manager.c`）
- 完成底盘控制调度层（`chassis_control.c`）
- 完成直线闭环 PID 控制器

**此阶段遇到的主要困难：**

| 问题 | 表现 | 解决方案 |
|------|------|---------|
| USART 分配错误 | 电机驱动被误分配到 USART3，与 PCB 硬件设计（USART1）不符 | 审查后全部迁移到 USART1 |
| 系统时钟配置错误 | 未启用 PLL，系统时钟仅运行在 8MHz（理论最高 72MHz） | 后续需改为 HSE+PLL=72MHz |
| RS485 方向控制 | 开发板未引出方向控制 GPIO 引脚 | 保持硬件自动收发模式，不添加 GPIO 控制 |
| 电机反馈读取为空 | `MotorManager_UpdateFeedback()` 函数为空实现 | 后续依据"只发不收"策略移除空函数 |

### 第四阶段：工程审查与修复（2026-06-09 ~ 2026-06-17）

- 第一轮项目审查（2026-06-09）：发现 USART 分配错误、时钟配置问题、栈空间不足等
- 第二轮项目审查（2026-06-09）：修复 USART1 迁移，调整参数
- 第三轮工程纠错（2026-06-16~17）：工程变量规范化，移除测试残留

**主要变更：**
- 电机串口从 USART3 迁移至 USART1（与 PCB 硬件一致）
- 移除临时测试模块（`uart3_sensor_stream_temp`、`bringup_mode`）
- 新增 CSV 数据调试输出功能
- 新增树莓派远程命令协议框架
- 文档统一与工程简化

### 第五阶段：固定路线与功能集成（2026-06-17 ~ 当前）

- 完成固定路线执行模块（`competition_route`）
- 实现 12 步比赛路线（前进、横移、旋转、停止）
- 新增 IMU 辅助直线纠偏功能
- 移除激光模块（由于编译依赖问题及硬件限制）
- 实现串口信号共享调试

**当前最新工程状态：**
- 最新工程版本：`smart_logistics_car_integrated_route_2026-06-17`
- 运行模式：`FIXED_ROUTE_STANDALONE_ENABLE = 1U`（独立固定路线模式）
- 路线控制：开环时间控制（后续可升级为 IMU 闭环旋转）

---

## 三、现有可实现功能

### 3.1 基础运动功能 ✅

| 功能 | 状态 | 说明 |
|------|------|------|
| 上电自动运行固定路线 | **可用** | 200ms 启动等待后自动开始 |
| 前进 | **可用** | 默认 0.18 m/s |
| 后退 | **可用** | 默认 0.18 m/s |
| 左/右横移 | **可用** | 默认 0.14 m/s |
| 原地旋转 | **可用** | 开环时间控制，默认 0.45 rad/s |
| 停止 | **可用** | 路线结束或手动停止 |
| 四轮速度解算 | **可用** | 麦轮逆运动学，等比限幅 |
| 四电机同步使能与速度下发 | **可用** | Modbus 单播 + 广播同步触发 |

### 3.2 传感器与闭环功能 ⚡

| 功能 | 状态 | 说明 |
|------|------|------|
| JY61P 陀螺仪读取 | **代码就绪，当前独立模式下未启用** | 支持 yaw/pitch/roll、三轴加速度、三轴角速度 |
| IMU 软件零偏校准 | **代码就绪** | 静止 200 样本自动校准 |
| IMU 航向融合 | **代码就绪** | 陀螺积分 + 模块 yaw 互补修正 |
| 直线航向纠偏 | **部分启用** | 新增直线段 yaw 闭环纠偏 |
| 激光测距 | **已移除** | 因编译依赖和硬件限制移除 |
| 电机速度反馈 | **已放弃** | 硬件 RS485 不支持接收，改为"只发不收"策略 |

### 3.3 通信与控制功能 📡

| 功能 | 状态 | 说明 |
|------|------|------|
| Modbus-RTU 电机控制 | **可用** | 发送使能、速度、停止指令 |
| CSV 串口调试输出 | **可用** | 10Hz 全量传感器数据流 |
| 树莓派遥控协议 | **代码就绪** | USART2 命令协议：VEL、STOP、STAT?、IMU? 等 |
| 传感器快照系统 | **可用** | 统一传感器数据接口 `Sensor_GetSnapshot()` |

---

## 四、独具特色功能

### 4.1 麦克纳姆轮全向移动

采用 X 形布局麦克纳姆轮，配合精确的逆运动学解算（`Mecanum_InverseKinematics`），实现：

- **前进/后退**：四轮同向同速
- **左右横移**：前后轮差速
- **原地旋转**：对角同向、同侧反向
- **任意矢量运动**：任意方向平移 + 旋转的复合运动

**关键技术参数：**
- 轮径：75mm
- 轮距：320mm
- 轴距：220mm
- 最大平移速度：0.60 m/s
- 最大旋转角速度：1.50 rad/s
- 加速度限制：2000 RPM/s

### 4.2 Y 轴速度规划与路线

代码中包含完整的固定路线规划，支持：
- **直线前进/后退距离控制**：速度 × 时间换算
- **横移/斜向运动**：vx/vy 任意比例组合
- **原地旋转角度控制**：开环时间控制
- **复合运动**：平移 + 旋转可同时叠加

### 4.3 模块化传感器架构

项目的传感器驱动采用分层设计，具有良好可扩展性：

```
传感器硬件驱动层（jy61p.c / atk_ms53l1m.c）
        ↓
传感器应用层（imu_app.c / laser_app.c）
        ↓
传感器集成层（sensors.c） → Sensor_GetSnapshot()
        ↓
传感器桥接层（sensor_bridge.c）→ 统一数据类型
```

这种架构使更换传感器或增减模块非常方便，仅需修改对应驱动文件即可。

### 4.4 实时 CSV 调试数据流

项目提供了独立的 CSV 调试数据输出功能，可通过 USART1 实时回传以下数据：

```
时间戳(ms), 加速度X(g), 加速度Y(g), 加速度Z(g),
角速度X(dps), 角速度Y(dps), 角速度Z(dps),
Roll(°), Pitch(°), Yaw(°)
```

- 输出频率：10Hz（不干扰主控制周期）
- 条件编译控制：`CSV_DEBUG_ENABLE` 一键开关
- 可用于示波器、Excel 导入或上位机实时可视化

### 4.5 软件定义的控制模式切换

通过宏定义实现灵活的控制模式切换：

| 宏开关 | 作用 |
|--------|------|
| `FIXED_ROUTE_STANDALONE_ENABLE` | 独立固定路线模式 / 完整传感器+树莓派模式 |
| `COMPETITION_ROUTE_AUTO_START` | 上电自动启动 / 等待外部信号启动 |
| `COMPETITION_ROUTE_USE_IMU_ROTATION` | 旋转开环时间控制 / IMU 闭环角度控制 |
| `CSV_DEBUG_ENABLE` | 调试数据流开启/关闭 |

---

## 五、项目推进过程中遇到的困难与解决方案

### 5.1 硬件层面

| 困难 | 表现 | 解决方案 |
|------|------|---------|
| **RS485 半双工只有发送能力** | 硬件未引出 RS485 方向控制 GPIO，无法接收电机状态回传 | 采用"只发不收"开环控制策略，放弃电机反馈读取 |
| **激光模块编译依赖缺失** | VL53L1X API 库未纳入工程，导致编译错误 | 移除激光模块，取消激光避障/急停保护 |
| **开发板 C 口连接 UART2** | TypeC 接口通过 CH340N 连接到 USART2，非直连 | 配置 USART2 与树莓派通信，波特率 115200 |

### 5.2 软件层面

| 困难 | 表现 | 解决方案 |
|------|------|---------|
| **USART 分配错误** | 电机驱动代码被错误分配至 USART3，与 PCB（USART1）不符 | 审查后迁移至 USART1，确保与硬件一致 |
| **系统时钟 8MHz** | 未配置 PLL，CPU 仅运行在 1/9 理论性能 | 后续需改为 HSE+PLL=72MHz |
| **上电自动行驶安全隐患** | Standalone 模式下 200ms 后自动启动路线 | 开发调试时关闭 AUTO_START，比赛时开启 |
| **栈空间不足** | 链接脚本仅分配 1024 字节栈空间 | 增大至 2048 字节 |
| **多套传感器快照结构冗余** | `SENSOR_Snapshot_t` 与 `SensorSnapshot_t` 重复 | 可统一为一套结构体 |
| **Vibe Coding 代码质量问题** | 部分提示词生成的代码存在逻辑瑕疵 | 通过多轮工程审查+修复的方式持续改进 |

### 5.3 工程管理层面

| 困难 | 表现 | 解决方案 |
|------|------|---------|
| **文档版本不统一** | 多个版本的硬件手册、设计文档并行 | 统一使用 V3 版硬件手册作为最高标准 |
| **临时测试模块残留** | `uart3_sensor_stream_temp`、`bringup_mode` 等模块未清理 | 审查后逐一清理，干净工程归档 |
| **变量命名不规范** | 不同模块中变量命名风格不一 | 生成工程变量总览文档，后续统一规范 |

---

## 六、项目成果总结

### 6.1 完成度评估

| 维度 | 完成度 | 说明 |
|------|--------|------|
| 底盘运动控制 | **90%** | 麦轮运动学、四电机同步、速度规划已完成 |
| 传感器系统 | **70%** | IMU 驱动就绪、闭环纠偏部分启用；激光已移除 |
| 通信链路 | **60%** | Modbus 电机控制已完成；树莓派协议代码就绪但未完全联调 |
| 路线执行 | **85%** | 固定路线按时间控制可用，暂未启用 IMU 闭环旋转 |
| 功能调试 | **75%** | CSV 调试、工程审查、参数调优基本完成 |

### 6.2 可展示亮点

1. **四轮独立控制**：四个 ZDT-X42-V2 步进电机通过 RS485 总线独立寻址、同步控制
2. **全向移动能力**：麦克纳姆轮配合精确运动学解算，实现任意方向平移和原地旋转
3. **模块化软件架构**：传感器→控制→通信严格分层，代码可维护性强
4. **丰富的调试手段**：CSV 数据流、状态回传、串口命令调试
5. **完整的版本控制**：Git 全程追踪，CHANGELOG 定期维护

### 6.3 待完善方向

1. **系统时钟修复**：启用 HSE + PLL 至 72MHz
2. **IMU 闭环旋转**：启用 `COMPETITION_ROUTE_USE_IMU_ROTATION`，提高旋转精度
3. **树莓派联调**：完成 USART2 命令协议的全链路验证
4. **场地实测**：在实际比赛场地上标定路线参数
5. **视觉识别集成**：通过树莓派摄像头实现物料识别与定位

---

## 七、附录：目录结构与关键文件索引

### 项目源码目录（主要部分）

```
smart_logistics_car_integrated/
├── Core/
│   ├── Inc/                    # 头文件
│   │   ├── config.h            # 全局配置宏
│   │   ├── mecanum.h           # 麦轮 API
│   │   ├── mecanum_kinematics.h # 运动学解算
│   │   ├── mecanum_config.h    # 运动参数配置
│   │   ├── mecanum_types.h     # 数据类型定义
│   │   ├── chassis_control.h   # 底盘控制
│   │   ├── motor_manager.h     # 电机管理
│   │   ├── modbus_rtu.h        # Modbus 协议
│   │   ├── zdt_x42_modbus.h    # ZDT 电机协议
│   │   ├── jy61p.h             # JY61P 驱动
│   │   ├── imu_app.h           # IMU 应用层
│   │   ├── sensors.h           # 传感器集成
│   │   ├── sensor_bridge.h     # 传感器桥接
│   │   └── straight_control.h  # 直线闭环控制
│   └── Src/                    # 源文件
│       ├── main.c              # 主程序入口
│       ├── mecanum_kinematics.c
│       ├── mecanum.c
│       ├── chassis_control.c
│       ├── motor_manager.c
│       ├── modbus_rtu.c
│       ├── zdt_x42_modbus.c
│       ├── jy61p.c
│       ├── imu_app.c
│       ├── sensors.c
│       ├── sensor_bridge.c
│       └── straight_control.c
├── UserModules/
│   ├── competition_route/      # 固定路线执行器
│   └── remote_cmd/             # 树莓派远程命令协议
├── Drivers/
│   └── VL53L1X/                # VL53L1X API（已被移除）
├── Docs/                       # 项目文档
├── Templates/                  # 代码模板
└── Debug/                      # 编译输出
```

### 关键文档索引

| 文档 | 位置 | 用途 |
|------|------|------|
| README.md | 项目根目录 | 项目简介与目录结构 |
| CHANGELOG.md | 项目根目录 | 版本更新记录 |
| 硬件平台技术手册 V3 | `代码/` | 硬件设计最高标准 |
| 麦轮运动控制系统设计文档 5Pro | `代码/` | 运动控制系统详细设计 |
| 工程审查报告 2026-06-09 | `VibeCoding/工程纠错/` | 第一轮代码审查 |
| 智能物流小车工程审查报告 | `VibeCoding/Docs/` | 第二轮详细审查 |
| 628 问题审查 | `VibeCoding/工程纠错/` | 工程现状与问题梳理 |
| 629 工程整改指南 | `VibeCoding/工程纠错/` | 问题修复指导 |
| 工程变量总览 2026-06-16 | `VibeCoding/工程纠错/` | 变量命名规范 |

---

*本文档由星尘光 StaRacy 于 2026-07-05 整理，基于项目推进过程资料、多个版本的技术文档与工程审查报告综合编写。*
